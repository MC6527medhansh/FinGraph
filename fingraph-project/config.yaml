project:
  name: "FinGraph"
  version: "1.0.0"
  author: "Your Name"
  description: "Financial Risk Assessment with Graph Neural Networks"

data:
  start_date: "2020-01-01"
  end_date: "2024-09-01"
  max_companies: 500
  update_frequency: "daily"
  
apis:
  yahoo_finance:
    rate_limit: 2000  # requests per hour
  fred:
    api_key: null  # Set this later
  twitter:
    api_key: null  # Set this later

model:
  hidden_dim: 128
  num_heads: 8
  num_layers: 3
  dropout: 0.2
  learning_rate: 0.001

paths:
  data_raw: "data/raw"
  data_processed: "data/processed" 
  models: "models"
  outputs: "outputs"